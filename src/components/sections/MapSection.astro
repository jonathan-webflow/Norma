<section class="bg-canvas py-section">
  <div class="mx-auto max-w-container px-4">

    <!-- Wrapper de container-query: 1cqw = 1% da largura do card -->
    <div style="container-type: inline-size;">

      <!-- Card — mapa escala proporcional via cqw (mesmo enquadramento do Figma) -->
      <div
        class="map-cta relative overflow-hidden rounded-xl flex items-center justify-center"
        style="min-height: clamp(20rem, 12rem + 24vw, 36.75rem); background-color: var(--color-surface); background-image: url('/images/map-dots.svg'); background-repeat: no-repeat;"
      >

        <!-- Content layer -->
        <div class="relative z-10 flex flex-col items-center justify-center gap-8 px-[clamp(1.5rem,0.5rem+5vw,4rem)] py-12 text-center">
          <h2 class="font-display text-display-2 text-ink max-w-[52rem] leading-tight" data-reveal="block">
            Proteja sua margem<br class="hidden md:inline">na Reforma Tributária.
          </h2>

          <p class="font-sans text-body-xl text-ink-muted max-w-[44rem]" data-reveal="block">
            Receba uma avaliação inicial da prontidão da sua operação para IBS/CBS.<br class="hidden md:inline">
            Sem custo. Sem compromisso. Você decide o que fazer depois.
          </p>

          <a
            href="#contato"
            class="inline-flex items-center justify-center bg-brand-dark text-ink-inverse font-display text-[1rem] tracking-[0.125em] whitespace-nowrap px-8 h-12 rounded-md"
            data-char-btn
          >
            Solicitar diagnóstico
          </a>
        </div>

      </div>
    </div>
  </div>
</section>

<style>
  /* Mobile: cover fills the full card height so the map is always visible */
  .map-cta {
    background-size: cover;
    background-position: center center;
  }

  /* Desktop: exact Figma framing via container query units */
  @media (min-width: 48rem) {
    .map-cta {
      background-size: 109.25cqw auto;
      background-position: -4.62cqw -8.67cqw;
    }
  }
</style>
